{
  "name": "trastocker",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "build:libraries": "turbo run build --filter=@trastocker/*-definition --filter=@trastocker/*-helper --filter=./libraries/*",
    "clean": "turbo run clean && pnpm run clean:self",
    "clean:self": "rimraf .next node_modules dist .turbo",
    "create:package": "turbo gen package && pnpm install",
    "dev": "turbo run dev",
    "lint:code": "turbo lint:code",
    "lint:type": "turbo lint:type",
    "sort:package": "sort-package-json 'package.json' 'apps/*/package.json' 'definitions/*/package.json' 'helpers/*/package.json' 'libraries/*/package.json' 'turbo/*/package.json'",
    "test": "turbo run test --filter=@trastocker/app-*"
  },
  "devDependencies": {
    "@praha/eslint-config-definer": "^2.0.1",
    "@turbo/gen": "^2.5.5",
    "@types/node": "^22.16.4",
    "eslint": "9.25.0",
    "jiti": "^2.4.2",
    "lefthook": "^1.12.2",
    "rimraf": "^6.0.1",
    "sort-package-json": "^2.15.1",
    "turbo": "^2.5.5",
    "wrangler": "^4.25.0"
  },
  "packageManager": "pnpm@10.9.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=10.9.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "drizzle-orm": "patches/drizzle-orm.patch"
    }
  }
}
